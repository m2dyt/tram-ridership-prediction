# Очередь задач: модель, данные и backend

Этот файл — рабочий checklist после анализа в [HACKATHON_READINESS_PLAN.md](HACKATHON_READINESS_PLAN.md). Задачи ограничены источниками данных, моделью, обучением, inference API, проверкой качества и документацией запуска. Frontend не рассматривается.

Сначала закрывать задачи по порядку. Каждая задача заканчивается коротким review: проверить предпосылки, найти утечки/неверные нули, сверить метрики и обновить документы. Не переходить к сложной модели, если не закрыт контракт данных или baseline.

## Очередь

| ID | Этап | Задача | Зависит от | Готово, когда |
|---|---:|---|---|---|
| DATA-01 | 0 | Сверить актуальные правила, deadline, sample и лимит попыток на платформе; сохранить лучший score/файл. | — | Локально записаны дата проверки, receipt/score и точный sample; лучший файл отдельно сохранён. |
| DATA-02 | 1 | Скачать конкурсный архив из [официально переданной ссылки](https://disk.yandex.ru/d/DiFwlfMOauxjBg), сохранить исходники и словарь в `sources/` с лицензией, временем и SHA-256. | DATA-01 | Архив воспроизводимо распаковывается; все имена/хеши и паспорт зафиксированы; секреты не сохранены. |
| DATA-03 | 1 | Составить data profile: схема, уникальные ключи, пропуски/дубли, нули, диапазоны, coverage по маршруту/дате/часу, sample-vs-test. | DATA-02 | Машинный отчёт; правила `missing`/`zero` обоснованы словарём; все строки test имеют определённый ключ. |
| MODEL-01 | 1 | Реализовать/зафиксировать точный WAPE-score и baseline на официальных локальных split. | DATA-03 | Формула совпадает с регламентом, нулевой знаменатель обработан явно; прогнозы и метрики сохранены с хешами. |
| MODEL-02 | 2 | Разобрать `train_tram.py`: выделить чистые функции grid, календаря, профиля, остатков, validation и submission без изменения результата. | MODEL-01 | Скрипт импортируемый, CLI воспроизводим, входы/выходы явны; эталонное CSV совпадает по ключам/порядку/правилам. |
| MODEL-03 | 2 | Проверить leakage и `fillna(0)`: тесты для неполного покрытия, дублей, будущих дат, лагов, train/test temporal boundary. | MODEL-02 | Отсутствующие метки отличимы от истинного нуля; целевые значения после cutoff не участвуют в обучении. |
| MODEL-04 | 2 | Сравнить baseline и текущие ручные коэффициенты/остаточную модель на закреплённом temporal validation и rolling-origin folds. | MODEL-03 | Есть полные прогнозы и WAPE-score по общему потоку, каждому маршруту/часу/периоду; выбрана версия по validation. |
| DATA-04 | 3 | Получить полный часовой погодный архив на покрытие конкурса из Open-Meteo Historical Weather и сохранить provenance. | DATA-03 | Покрыты train/validation/test; единицы/timezone/модель/параметры зафиксированы; нет тихого forward-fill. |
| DATA-05 | 3 | Построить таблицу выходных/праздников/переносов из официальных постановлений РФ (версии 2025–26). | DATA-03 | Единая таблица дат проверена; официальные переносы отделены от обычной субботы/воскресенья; исходник и правила сохранены. |
| MODEL-05 | 3 | Провести ablation: baseline, +calendar, +weather, +calendar+weather, одинаковые временные точки и coverage. | DATA-04, DATA-05 | Улучшение/ухудшение измерено; источник включается только если устойчиво помогает. Отчёт содержит доверие/ограничения. |
| DATA-06 | 3 | Проверить возможность получить исторический трафик и архив событий/анонсов; оценить покрытие и время публикации до скачивания больших данных. | DATA-03 | Решение go/no-go документировано; нет source claim без файла/API и пригодного времени доступности. |
| MODEL-06 | 3 | Если DATA-06 go: отдельно протестировать traffic и events, затем combined ablation на route/global уровне. | DATA-06 | Для каждого источника есть audit trail, доля покрытия, same-split метрика и решение оставить/удалить. |
| MODEL-07 | 4 | Исправить/отдельно пометить metro training path: `line_name` mapping, sklearn categorical contract, artifact overwrite/provenance. | — | Metro pipeline не путается с конкурсным; конфиг train/evaluate действительно выполняется; отчёт подписан как эксперимент. |
| BACKEND-01 | 4 | Утвердить artifact contract: version, model family, feature schema, training cutoff, data hashes, dependency versions, evaluation, checksum. | MODEL-04 | Есть schema validation и immutable model directory; неполный артефакт не загружается. |
| BACKEND-02 | 4 | Добавить predictor adapter на границе application/infrastructure; композиция worker может выбрать версионированный ML Predictor или explicit seasonal fallback. | BACKEND-01 | Домен не импортирует CatBoost/LightGBM/sklearn; model fit отсутствует в API; версия видна в результате. |
| BACKEND-03 | 4 | Обеспечить одинаковые feature transforms между batch validation и online prediction; прописать `as_of`, timezone и будущие значения. | BACKEND-02 | Для одной точки одинаковый вход даёт одинаковые признаки/прогноз; нет фактических будущих лагов и погодных наблюдений в live mode. |
| BACKEND-04 | 4 | Добавить CLI train/evaluate/export и управление моделями вне web процесса. | BACKEND-01 | Команды документированы, принимают явные пути/config, возвращают ненулевой код на ошибке и не перезаписывают принятую модель молча. |
| BACKEND-05 | 5 | Добавить API/integration проверки artifact loading, response metadata, fallback, плохой схемы, неизвестного маршрута/недостающего контекста. | BACKEND-02, BACKEND-03 | Тесты закрывают главные контракты на локальных fixtures без сети и внешней БД, где применимо. |
| BACKEND-06 | 5 | Выполнить нагрузочный замер и записать окружение, данные, p50/p95/p99, throughput, ошибки, CPU/RAM. | BACKEND-05 | В README только реальные измерения; PDF target не выдаётся за результат. |
| DOC-01 | 6 | Обновить README, STATUS, file map, ML/source/backend инструкции; убрать устаревшие «обучение отсутствует» и неподтверждённые проценты. | BACKEND-04, MODEL-05 | Новый разработчик может найти данные, воспроизвести validation, обучить, экспортировать артефакт, запустить API и понять ограничения. |

## Порядок следующего инженерного шага

1. **DATA-01:** сверить платформу и зафиксировать лучший текущий результат. Это защищает от случайной потери рабочего варианта перед дедлайном.
2. **DATA-02–03:** сначала подтвердить содержание/качество конкурсного архива и смысл меток; не менять алгоритм по одному score.
3. **MODEL-01–04:** закрепить baseline и сделать аудит текущих конкурсных поправок/временной валидации.
4. **DATA-04–05 и MODEL-05:** включить погодный архив и официальный календарь только с ablation. Это наиболее выполнимые дополнительные источники.
5. **DATA-06:** выяснить наличие реального исторического трафика и событий; пока доступа/архива нет, не блокировать модель на этом.
6. **BACKEND-01–06:** только после утверждения feature/model contract подключить готовый обученный артефакт к worker/API.
7. **DOC-01:** синхронизировать инструкции со сделанным и записать финальную review/проверки.

## Существующая структура и предполагаемая ответственность

| Путь | Текущее назначение | Следующий шаг |
|---|---|---|
| `sources/` | неизменяемые оригиналы, паспорта, provenance; исключены из Git по `.gitignore` | добавить конкурсный архив и новые внешние снимки с датой/хешем |
| `data/` | нормализованные, подготовленные версии и отчёты качества; локальные данные игнорируются | добавить отдельный `data/training/<version>/` для конкурсных feature sets/splits |
| `ml/training/train_tram.py` | существующий конкурсный путь route×date×hour и генерация CSV | разделить ответственности и добавить проверяемую конфигурацию/отчёты |
| `ml/training/prepare.py`, `train.py`, `evaluate.py` | отдельный квартальный metro pipeline | исправить расхождения и обозначить его как эксперимент, не смешивать его датасет/score с трамвайной задачей |
| `ml/src/tram_ml/` | переиспользуемые доменно-независимые baseline/features/evaluation/context | добавлять общие детерминированные трансформации только после контрактного ревью |
| `models/` | место версий, model cards и артефактов, вероятно игнорируется Git | проверить фактические правила хранения; записывать immutable manifests и model card |
| `backend/src/tram/application/ports.py` | `Predictor` protocol и `Repository` ports | сохранить этот seam; не импортировать библиотеки обучения в домен |
| `backend/src/tram/composition.py` | сборка API/worker; worker сейчас использует `SeasonalNaive()` | внедрить artifact loader/adaptor после фиксации artifact contract |
| `openapi.yaml` | контракт внешнего HTTP API | описать модельную версию/as-of, если это соответствует реальному API; обновлять вместе с endpoint |
| `requirements.txt`, `pyproject.toml` | единый runtime/dev набор | добавить CPU inference/training зависимости согласованно; optional extra предпочесть второму `requirements*.txt` |

## Определение готовности одной задачи

Перед изменением записать короткий план из 3–6 пунктов. После реализации выполнить review с рисками/пограничными случаями, затем релевантные unit/API checks; для изменения модели сохранить прогнозы, конфиг, split и метрики. Не запускать обучение на финальном test и не считать network/API HTTP 200 доказательством пригодного датасета. Обновить README/документ конкретной папки. После этого зафиксировать изменение отдельным осмысленным commit; не переписывать историю и не отправлять результат на площадку без отдельного запроса.
