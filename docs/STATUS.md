# Состояние реализации и реальные ограничения

Обновлено 21.09.2026. Само обучение моделей не выполняется по запросу пользователя; его следующий этап описан в [TRAINING_PLAN.md](TRAINING_PLAN.md). Удалённый CI и промышленное развёртывание не выполнялись.

| Требование исходного ТЗ | Реализовано | Что ещё зависит от данных/доступа |
|---|---|---|
| F01 — подготовка | Потоковый JSONL/CSV агрегатов, неизменяемые ревизии, проверки сетки/пропусков/дублей, атомарная публикация | Универсальный ETL сырых валидаций/телематики невозможен без файлов и словаря полей; сопоставление поездок не выдумывается |
| F02 — три горизонта | Сезонная база день/месяц/год, очередь и worker | Обученная модель, реальные допустимые даты и качество |
| F03 — фильтры | Маршрут, профиль, период, пространственный ряд остановки/участка; карта и график связаны | Детализация зависит от опубликованных профилей |
| F04 — карта | React + Leaflet + OSM, геометрии сети/прогноза, геообъекты, маркировка демо | Реальная историческая сеть Москвы ещё не импортирована |
| F05/F06 — статистика/обновления | Графики, таблицы, CSV, сохранённые расчёты, время отсечки, polling очереди, ручное обновление | Источник живой телематики отсутствует; это не реальный поток трамваев |
| F07 — качество | Полные временные folds, MAE/WAPE, all/peak/route, отчёты и точки | Оценена только сезонная база на синтетических рядах |
| F08 — интервалы | Контракт допускает интервалы | Не калибровались, значение null |
| F09 — внешние факторы | HTTP-адаптеры, GeoJSON Москвы, снимки, признаки с as_of | Вклад признаков не измерен, сезонная база их не использует |
| F10 — предупреждения | Переполнение, отсутствующие данные, ошибки провайдеров, ограничения | Нет согласованных диспетчерских порогов и доставки оповещений |
| F11 — выпуск | Сценарный калькулятор с резервом и допущениями | Нет оптимизации, ограничений путевой инфраструктуры, расписания и подачи |
| F12 — выходы/остаток | Групповой баланс short/uniform/long, журнал, replay, измеренные высадки, экспорт агрегатов | Индивидуальные поездки не идентифицируются; распределение не проверено на реальных выходах |
| F13 — объяснение | Метод базы, отсечка, происхождение, ограничения | Объяснения влияния признаков обученной модели ещё невозможны |
| F14/F15 — рекомендации/городские сценарии | Базовые строительные блоки и ручные сценарии | Полноценный оптимизатор/модель инфраструктурных изменений не реализованы |

## Проверенные источники

- Open-Meteo: успешный живой запрос, сохранение и отображение 72 часовых записей текущего выпуска. Для документации дополнительно скачан архивный образец из 168 часов; это отдельный файл, не публикация в БД.
- KudaGo: адаптер проверен на ответах-фикстурах; после первоначального ConnectTimeout повторный прямой запрос вернул HTTP 200. Сохранён образец 5 событий первой страницы, полный каталог не загружен.
- Timepad: анонимный живой запрос вернул 403. Параметр токена предусмотрен; действующий токен не предоставлен.
- WeatherAPI: адаптер/обработка ошибок проверены на фикстурах; действующий ключ не предоставлен.
- data.mos.ru: наборы 624/62743 из спецификации пользователя скачаны через браузерный экспорт, вместе с паспортами: 1 177 и 6 448 записей. Первичный аудит полей/периода/качества выполнен; нормализованные ряды и сопоставление ID ещё не подготовлены. Прямой HTTP возвращает HTML-оболочку, API — 401 с требованием api_key.
- OSM/Geofabrik: официальная страница выгрузки Центрального ФО прочитана исследовательским инструментом; локальный запрос завершился таймаутом. Полный архив не скачивался и в сеть проекта не импортирован.

Подробности, точные URL, локальные файлы и ограничения — [DATASETS.md](DATASETS.md), [DATA_AUDIT.md](DATA_AUDIT.md) и [DATA_LINK_CHECKS.json](DATA_LINK_CHECKS.json). Квартальный эксперимент метро ещё не реализован: текущая сетка hour/day/month не поддерживает кварталы. Нет команды train, артефакта модели или его загрузчика.

## Эксплуатационные границы

Локальный запуск: API/worker в .venv + PostgreSQL в Docker. SQLite — тестовый режим. Bearer viewer/operator подходят для закрытого прототипа; для внешнего размещения нужны HTTPS, управление пользователями и секретами, наблюдаемость, резервное копирование, защита периметра и проверка лимитов нагрузки. Ключи не встраиваются во frontend.

Размер HTTP-команды ограничен 64 KiB, ответа провайдера — 5 MB, GeoJSON — 50 MB. История/точки запрашиваются окнами до 366 дней. Внешние запросы выполняются по явному действию; автоматический планировщик/бесконечные повторы не запускаются. Каталоги мероприятий ограничены 100 событиями за запрос и не являются полным перечнем событий города.

Python-зависимости заданы совместимыми диапазонами в pyproject/единственном requirements.txt; это не полный транзитивный lock. npm использует точные версии и package-lock.json. Известны два предупреждения об устаревающих интерфейсах зависимостей FastAPI/TestClient; тесты проходят, несовместимых пакетов pip check не обнаруживает.

Dockerfile включает frontend и backend. Предыдущая и итоговая доступность контейнерной сборки фиксируется в [журнале проверок](IMPLEMENTATION_LOG.md); наличие Dockerfile само по себе не означает успешную сборку в закрытой сети.


## Проверка реализации до обновления документации

70 Python-тестов без пропусков, 3 Node-теста, Ruff/Prettier, Vite build, миграции, pip check и ссылки документации прошли. Реальный API/worker на PostgreSQL выдал 24/30/12 точек на day/month/year. Docker: frontend собран, Python-слой заблокирован сетевым тайм-аутом PyPI/hatchling. Локальный способ запуска в README проверен.

Последующее обновление README меняет документацию, реестр ссылок и правила исключения локальных артефактов. Проверки этой редакции отдельно фиксируются в IMPLEMENTATION_LOG; результаты выше не являются новым запуском обучения или всей тестовой системы.
