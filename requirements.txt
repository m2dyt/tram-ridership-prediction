# Application and development dependencies from pyproject.toml.
# Supported project interpreter: Python 3.12 (3.13 is also permitted).
# These are version ranges, not a reproducibility lock file.
alembic>=1.14,<2
fastapi>=0.115,<1
jsonschema>=4.23,<5
psycopg[binary]>=3.2,<4
pydantic-settings>=2.7,<3
pyyaml>=6,<7
sqlalchemy>=2.0,<3
tzdata>=2025.1
uvicorn>=0.30,<1

# API tests, coverage, lint and formatting.
httpx>=0.28,<1
pytest>=8.3,<10
pytest-cov>=6,<8
ruff>=0.11,<1
