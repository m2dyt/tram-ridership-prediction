# Трамвай · прогноз загрузки

Рабочий исследовательский прототип: **FastAPI + PostgreSQL + отдельный worker, React на JavaScript + Vite + OpenStreetMap/Leaflet**. Есть сезонный прогноз на сутки, месяц и год без обучения, история и отчёты качества, расчёт остатка пассажиров по вагонам, внешние снимки погоды/мероприятий, импорт геоданных и сценарный калькулятор выпуска.

**Текущий этап: приложение работает на демоданных; следующий этап — подготовить реальные данные и только затем обучать модель.** Обучение ещё не выполнялось. Сезонная база строит прогноз по фиксированному правилу, без `fit`. Демонстрационные маршруты и пассажиропоток синтетические. [Таблица готовности и ограничений](docs/STATUS.md).

В твоей спецификации `SPECIFICATION (2).md` были ссылки на наборы Москвы **624 и 62743**. **Оба скачаны в локальный проект:** 1 177 записей входов метро и 6 448 записей квартального потока за 2021-Q1 — 2026-Q2. Архивы, паспорта и контрольные суммы находятся в `sources/data-mos/`; [результаты первичного аудита](docs/DATA_AUDIT.md). Есть также образцы погоды и мероприятий. Реальных трамвайных валидаций пока нет: география и поток метро сами по себе не дают почасовую наполненность трамвая.

## Что делать дальше

Если проект уже установлен, **получение исходников метро уже выполнено — начать с выбора задачи и подготовки рядов, пункты 2–3**. После нового клонирования выгрузки нужно получить заново: они не коммитятся. Если запускаешь впервые — инструкция ниже в разделе «Быстрый запуск».

| Шаг | Что конкретно сделать | Где работать и что должно получиться |
|---|---|---|
| 1. Получить исходники — выполнено для метро | Выгрузки **624 v6.8** и **62743 v1.35**, паспорта и SHA-256 сохранены. Поля и покрытие проверены; найдены нулевые значения, разрывы и неоднозначности сопоставления. | [sources/README.md](sources/README.md): файлы в `sources/data-mos/<id>/2026-09-21/`; [аудит и следующие действия](docs/DATA_AUDIT.md). |
| 2. Выбрать задачу | Если есть только 62743 — учебный прогноз **квартального потока метро**. Для цели проекта — получить посадки/выходы или датчики трамваев, сеть, рейсы и вместимости. | [TRAINING_PLAN.md](docs/TRAINING_PLAN.md): зафиксировать целевую величину, пространственную единицу, горизонт и доступность данных. Эти две задачи оцениваются отдельно. |
| 3. Подготовить набор | Сопоставить ID, проверить время, дубли, пропуски, изменения сети. Для наполненности восстановить баланс пассажиров с переносом оставшихся людей. | [data/README.md](data/README.md): отчёт качества, нормализованные таблицы; для поддерживаемых трамвайных рядов — `manifest.json` и `observations.jsonl`. Конвертер произвольного экспорта Москвы ещё нужно написать. |
| 4. Зафиксировать проверку | Разделить историю по времени на train/validation/test; выбрать полные контрольные горизонты. Посчитать сезонную базу на тех же рядах. | [ml/README.md](ml/README.md): список отсечек и отчёт базы. Текущий `evaluate` работает без обучения; квартального профиля метро в API пока нет. |
| 5. Обучить первую модель — будущая работа | После готовности данных реализовать отдельный процесс обучения: календарь + доступные лаги → простая регрессия; затем сравнить с более сложным кандидатом. Погоду и события добавлять по одному. | План кода, признаков и проверки — [TRAINING_PLAN.md](docs/TRAINING_PLAN.md). **Команды `train` сейчас нет**, обучающие библиотеки ещё не добавлены. |
| 6. Принять или отклонить модель | На нетронутом тесте сравнить MAE/WAPE, часы пик, маршруты и покрытие; проверить отсутствие информации из будущего. Оценённые высадки не считать измеренной истиной. | [models/README.md](models/README.md): версия модели, артефакт, схема признаков, паспорт, метрики и зависимости. Если улучшения нет — оставить базу. |
| 7. Подключить к приложению | Реализовать загрузчик артефакта через `Predictor`, регистрацию метода и выбор модели, обновить проверку качества и тесты. | [backend/README.md](backend/README.md). Один файл весов пока не подключает модель: publication/service/worker сейчас поддерживают только `seasonal_naive_v1`. Затем проверить отображение через [frontend](frontend/README.md). |

**Ближайший следующий результат:** согласованный квартальный ряд метро с устойчивыми ID, правилами для 193 нулевых строк и пяти внутренних разрывов, затем временное разбиение и сезонная база. Из 315 пар «станция + линия» 16 пока не совпали с геосправочником по точному названию; их нужно разобрать. Для трамвайной задачи отдельно получить реальные наблюдения. Обучается модель **на датасете**; датасет очищается, проверяется и версионируется.

## Датасеты: твои и дополнительные

Проверка ссылок: **21.09.2026**. HTTP 200 страницы не означает, что выгрузка набора проверена. Полный реестр, назначение каждого источника, порядок скачивания и результаты запросов — [DATASETS.md](docs/DATASETS.md); машинный журнал — [DATA_LINK_CHECKS.json](docs/DATA_LINK_CHECKS.json).

| Источник | Для чего нужен в проекте | Что проверено |
|---|---|---|
| **Твой набор [624 — входы и выходы вестибюлей метро](https://data.mos.ru/opendata/624)** | Географический признак близости пересадки. Вход, станция и пересадочный узел — разные сущности. | Карточка/паспорт открыты в браузере, JSON скачан: **v6.8, 1 177 записей**; координаты прошли структурную проверку. |
| **Твой набор [62743 — пассажиропоток метро](https://data.mos.ru/opendata/62743)** | Отдельный учебный эксперимент с квартальными агрегатами, как предусмотрено твоим ТЗ. | JSON скачан: **v1.35, 6 448 строк, 22 квартала**, 315 пар «станция + линия». Не делить кварталы на вымышленные часы. |
| **[Open-Meteo: архив погоды](https://open-meteo.com/en/docs/historical-weather-api)** — найден дополнительно | Температура, осадки, ветер для исследования влияния погоды. Для прогноза нужны сведения, доступные на его дату. | JSON HTTP 200, скачаны 168 часов Москвы за 01–07.01.2025; пропусков в выбранных полях нет. Это образец контекста, не обучающий набор пассажиров. |
| **[OpenStreetMap через Geofabrik, Центральный ФО](https://download.geofabrik.de/russia/central-fed-district.html)** — найден дополнительно | Геометрия трамвайной сети, остановки и городские объекты после фильтрации и проверки. | Страница прочитана исследовательским браузером; локальный запрос — таймаут. Полный PBF не скачан; импорт OSM в сеть проекта ещё не реализован. |
| **[KudaGo API](https://docs.kudago.com/api/)** — дополнительный источник событий | Анонсы, время и место мероприятий рядом с остановками. | Документация и JSON HTTP 200. Скачаны 5 записей из 423 в ответе за 21–27.09.2026 UTC; это только первая страница. |

Скачанные образцы лежат локально в `sources/open-meteo/2026-09-21/` и `sources/kudago/2026-09-21/`, рядом с каждым есть `provenance.json`. Они исключены из Git; после клонирования их можно получить по точным URL из реестра. Ключи для этих двух запросов не понадобились. Timepad в проверке вернул 403, API Москвы — 401 с требованием `api_key`; они не помечены как доступные выгрузки. [Погодные и событийные интеграции](docs/DATA_SOURCES.md).

## Как будет устроено обучение модели

```text
sources/          → data/                         → будущий процесс обучения
оригиналы + паспорт  очистка, целевой ряд, признаки   временные folds, fit, сравнение
                                                            ↓
frontend ← API ← worker + Predictor ← models/<версия>/ + решение о допуске
```

Для первого трамвайного эксперимента разумно ограничиться одним маршрутом, одним показателем и прогнозом на сутки. Если есть только валидации, целевой показатель — **посадки**, а не люди в вагоне. Для **наполненности** нужны рейс/вагон, порядок и время остановок, начальный остаток, входы/выходы либо явно обозначенная оценка выходов. Остаток переносится на следующий участок: `после = до + вошли − вышли`. Для доли заполнения нужна вместимость.

Признаки первой модели: календарь Москвы, прошлые значения, статистики только по известной истории; затем близость метро, погода и мероприятия. Для прогноза сразу на 24 часа нельзя подставлять фактические значения первых часов этих суток в лаги следующих. Разбиение случайным перемешиванием также непригодно. Подбор параметров выполняется на validation, финальная оценка — на отложенном test. [Подробный план обучения, требования к данным и изменения в коде](docs/TRAINING_PLAN.md).

Сейчас `evaluate` проверяет **сезонную базу**. Совпадение её результатов в полях `model` и `seasonal_naive` ожидаемо; это не сравнение с уже обученной моделью. Нулевые ошибки на синтетическом демо не доказывают точность для Москвы. Обучение, подбор параметров и артефакты весов в этой поставке не создавались.

## Быстрый запуск на Windows / PowerShell

Нужны Python **3.12**, Node.js **22.12+ (ветка 22) или 24+**, Docker Desktop для PostgreSQL. Выполнять из корня:

```powershell
cd C:\proga\projects\tram-ridership-prediction
```

**1. Создать venv, если его ещё нет:**

```powershell
py -3.12 -m venv .venv
```

**2. Активировать venv — отдельная команда:**

```powershell
.\.venv\Scripts\Activate.ps1
```

**3. Скачать и установить библиотеки — отдельная команда:**

```powershell
python -m pip install -r requirements.txt -e .
```

В проекте **один requirements.txt**. Он содержит Python-зависимости приложения и инструменты разработки; `-e .` подключает локальные пакеты tram и tram_ml. package.json/package-lock.json относятся к отдельному JavaScript frontend. Рабочую .venv пересоздавать не надо. Если активация ограничена политикой PowerShell, можно выполнять команды через `.\.venv\Scripts\python.exe`, не меняя политику системы.

**4. Создать конфигурацию только при отсутствии .env:**

```powershell
python -m tram.cli init-config
```

Генерируются случайный пароль БД и разные ключи viewer/operator/cursor. Существующий .env не перезаписывается. Ключи нужны для входа в интерфейс и Swagger; храните их локально. Шаблон параметров: [.env.example](.env.example).

**5. Поднять БД и применить миграции:**

```powershell
docker compose up -d --wait db
python -m tram.cli migrate
```

БД проекта доступна на 127.0.0.1:15433. Она отделена от тестовой БД на 15434. Если .env создавался ранее, сохраняйте его пароль и порт; не заменяйте пароль у уже заполненного Docker volume.

**6. Подготовить демонстрационные данные:**

```powershell
python -m tram.cli demo data/demo-complete
python -m tram.cli publish data/demo-complete
python -m tram.cli replay-trip data/demo-complete/trip-plan.json data/demo-complete/trip-events.jsonl
```

Если демоданные уже созданы, повторять demo в существующий каталог не нужно. Для нового дня выбрать новое имя каталога. Публикация одинакового содержимого и повтор журнала безопасны. [Отчёты, погода, импорт CSV/GeoJSON, экспорт наполненности](docs/OPERATIONS.md).

**7. Установить frontend:**

```powershell
cd frontend
npm.cmd ci
cd ..
```

**8. Запустить три компонента в отдельных терминалах:**

Терминал API из корня с активированной .venv:

```powershell
python -m tram.cli serve
```

Терминал worker из корня с активированной .venv:

```powershell
python -m tram.cli worker
```

Терминал frontend:

```powershell
cd frontend
npm.cmd run dev
```

Открыть **http://127.0.0.1:5173**. Ввести значение TRAM_OPERATOR_TOKEN из своего .env для создания расчётов либо TRAM_VIEWER_TOKEN для чтения. Ключ хранится только в памяти вкладки. Swagger — **http://127.0.0.1:8000/docs**, готовность — **http://127.0.0.1:8000/api/v1/health**. В Swagger нажать Authorize и вставить токен, без дополнительного слова Bearer.

Альтернатива командам отдельных терминалов: `./scripts/start-component.ps1 api`, `worker` или `frontend`. Скрипт запускает один компонент в текущем терминале; Ctrl+C останавливает его. После изменения Python-кода перезапустить API/worker. Frontend в dev обновляется автоматически; изменение файлов может сбросить вход.

## Сборка интерфейса и единый адрес

```powershell
cd frontend
npm.cmd run build
cd ..
python -m tram.cli serve
```

Если frontend/dist существует на момент запуска, FastAPI раздаёт его на **http://127.0.0.1:8000/**. API и Swagger продолжают работать по своим путям; отдельный Vite для готовой сборки не нужен. Worker остаётся отдельным процессом. После изменения frontend пересобрать dist. `npm run preview` предназначен для просмотра статической сборки и не настраивает API-прокси.

## Docker как альтернативный способ

Dockerfile собирает frontend и backend, Compose запускает миграции перед API/worker:

```powershell
docker compose up -d --build api worker
```

Перед этим остановить локальный API на 8000, чтобы не было конфликта портов. Для импорта данных в контейнер:

```powershell
docker compose run --rm -v "${PWD}/data:/imports:ro" migrate python -m tram.cli publish /imports/demo-complete
```

Локальная установка .venv проверена. Контейнерная сборка требует доступных npm/PyPI/registry; её фактический результат описан в [журнале](docs/IMPLEMENTATION_LOG.md). Не удалять postgres-data для обычного перезапуска.

## Как устроено решение

1. Исходные агрегаты проходят проверку и публикуются как неизменяемая ревизия. Неполные интервалы, неизвестные связи, дубли и будущая доступность отклоняются. Пропуски не превращаются в нули.
2. API создаёт задание. Worker использует сезонную базу: предыдущую доступную неделю для суток/месяца, предыдущий год для годового прогноза. Расчёт не обучает модель и не обращается к внешним API во время чтения.
3. Карта и график привязаны к одному run_id, пространственному ряду и интервалу. Доступные показатели/горизонты определяет опубликованный профиль, а не жёсткий список frontend.
4. Отдельный баланс рейса переносит группы пассажиров между остановками. Высадка оценивается по трём сценариям длительности либо задаётся измерением. Состояние и журнал сохраняются транзакционно. Наполненность и доля заполнения усредняются по времени проездов, не суммируются по вагонам.
5. Погода, мероприятия и московские геообъекты хранятся снимками с available_at. Признаки подготовлены для будущей модели; сезонный прогноз пока их не использует.
6. Оценка на истории использует сведения, доступные на момент отсечки. Отчёты содержат полные временные горизонты, MAE/WAPE и явные исключения.

Идея: сначала получить проверяемый поток данных и воспроизводимые расчёты, затем сравнивать обученные модели с честной базой. [Подробная архитектура и правила расширения](docs/ARCHITECTURE.md).

## Где что находится

| Папка/файл | Назначение |
|---|---|
| [backend](backend/README.md) | Python-приложение, HTTP, сценарии, domain и инфраструктурные адаптеры |
| [ml](ml/README.md) | Сезонная база, признаки и временная оценка без обучения |
| [sources](sources/README.md) | Неизменённые выгрузки и ответы источников; паспорт происхождения и порядок получения |
| [data](data/README.md) | Демо, очистка исходников, согласованные ряды, prepared bundle и будущие выборки обучения |
| [models](models/README.md) | Место будущих артефактов обучения; состав версии и условия подключения |
| [frontend](frontend/README.md) | React/JSX, API-клиент, карта, графики и формы |
| [tests](tests/README.md) | Domain, API, публикация, PostgreSQL, ML и изолированный browser smoke |
| [scripts](scripts/README.md) | Запуск компонентов и проверка документации |
| [docs](docs/README.md) | Архитектура, сценарии, форматы, источники, статус и ревью |
| [openapi.yaml](openapi.yaml) | Исполняемый контракт Swagger; соответствует HTTP-обработчикам |
| [IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md) | Исходный план проекта и дополнительные требования |
| [docs/DELIVERY_PLAN.md](docs/DELIVERY_PLAN.md) | План текущей поставки без обучения |
| [pyproject.toml](pyproject.toml) | Python-пакеты, совместимость, зависимости, CLI, pytest/Ruff |
| [requirements.txt](requirements.txt) | Единый список установки Python и инструментов |
| [alembic.ini](alembic.ini) | Настройка миграций; секретный URL берётся из .env |
| [compose.yaml](compose.yaml), [Dockerfile](Dockerfile) | БД, миграции, API/worker, многослойная сборка интерфейса |
| [.github/workflows/checks.yml](.github/workflows/checks.yml) | CI: Python/PostgreSQL и frontend |
| [.gitignore](.gitignore), [.dockerignore](.dockerignore), [.gitattributes](.gitattributes), [.python-version](.python-version) | Исключения секретов/артефактов, контекст Docker, окончания строк, версия Python |

README основных папок объясняют входы, обработку, результаты, файлы и следующий шаг. [Полная карта файлов](docs/FILE_MAP.md). Содержимое sources/, data/, models/ исключено из Git, кроме README; logs/, .venv/, node_modules/, dist/ и .env также не коммитятся.

## Проверка установки

```powershell
python -m tram.cli doctor
python -m pip check
docker compose --profile test up -d --wait test-db
```

Отдельно задать тестовую БД и выполнить проверки:

```powershell
$env:TRAM_TEST_DATABASE_URL = 'postgresql+psycopg://tram:tram-test-only@127.0.0.1:15434/tram_test'
python -m pytest -q
python -m ruff check backend ml tests scripts
python -m ruff format --check backend ml tests scripts
python scripts/check_docs.py
```

```powershell
cd frontend
npm.cmd run test
npm.cmd run build
npm.cmd run format:check
```

Без TRAM_TEST_DATABASE_URL интеграционные тесты PostgreSQL получают SKIPPED — это намеренно. [Полная инструкция проверок и изолированного UI-стенда](docs/TESTING.md).

## Как вести следующие изменения

Перед каждым шагом записать цель, входные данные, затрагиваемые файлы и критерий готовности. После выполнения — сразу ревью результата и проверок, затем исправление замечаний. История уже выполненной работы находится в [IMPLEMENTATION_LOG.md](docs/IMPLEMENTATION_LOG.md). Для продолжения обучения использовать [TRAINING_PLAN.md](docs/TRAINING_PLAN.md), а не считать исходный план подтверждением готовности ещё не написанного кода.
